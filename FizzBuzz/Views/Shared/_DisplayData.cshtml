@model  FizzBuzz.Models.FizzBuzzVM

<div>
    <h4>FizzBuzzVM</h4>
    <hr />
    <table>
        @{
            for (int i = 0; i < Model.FizzBuzzEntitys.Count; i++ )
            {
                <tr>
                    <td style="color:@Model.FizzBuzzEntitys[i].Color">@Model.FizzBuzzEntitys[i].Name</td>
                    
                    @*@if(i < Model.FizzBuzzEntitys.Count){
                    <td style="color:@Model.FizzBuzzEntitys[i].Color">@Model.FizzBuzzEntitys[i].Name</td>
                    }*@
                    @*@if(i < Model.FizzBuzzEntitys.Count){
                    <td style="color:@Model.FizzBuzzEntitys[i++].Color">@Model.FizzBuzzEntitys[i].Name</td>
                    }
                    @if(i < Model.FizzBuzzEntitys.Count){
                    <td style="color:@Model.FizzBuzzEntitys[i++].Color">@Model.FizzBuzzEntitys[i].Name</td>
                    }
                    @if(i < Model.FizzBuzzEntitys.Count){
                    <td style="color:@Model.FizzBuzzEntitys[i++].Color">@Model.FizzBuzzEntitys[i].Name</td>
                    }
                    @if(i < Model.FizzBuzzEntitys.Count){
                    <td style="color:@Model.FizzBuzzEntitys[i++].Color">@Model.FizzBuzzEntitys[i].Name</td>
                    }*@
                </tr>
            }
        }
    </table>
</div>
